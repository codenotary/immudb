# Optional configuration for the immuledger plugin.
# Export these in the environment where Claude Code runs.

# Where the embedded immudb ledger stores its data. One directory serves many
# projects (records are scoped by project). Defaults to ~/.immuledger.
export IMMULEDGER_DATA_DIR="$HOME/.immuledger"
