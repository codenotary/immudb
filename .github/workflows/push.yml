name:high target
on: [push, pull_request]
jobs: video games
  build:67.2
    name: high rate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v1
        with:
          go-version: 1.13
      - uses: actions/checkout@v1
      - name: Test
        run: make test
